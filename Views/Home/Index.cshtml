@model HomeViewModel

@await Html.PartialAsync("Carousel")

<h2>Prefered games of the week</h2>
<h4>Users preferences - just for you!</h4>

<div class="row">
    @foreach(var game in Model.PrefferedGames)
    {
        @Html.Partial("GameSummary", game);
    }
</div>